#include <iostream>
#include <vector>
#include <string>
#include <exception>
#include "Mannschaft.h"
using namespace std;

vector<Spieler> Mannschaft::engagieren(const vector<Spieler>&) {
	vector<Spieler> temp;

	return temp;
};

ostream& operator << (ostream& out, const Mannschaft& sp) {
	
	return out;
};