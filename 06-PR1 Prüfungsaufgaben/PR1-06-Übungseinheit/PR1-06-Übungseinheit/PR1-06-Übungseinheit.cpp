/*SchreibenSie eineFunktion voidrepl(string&str1,stringstr2,stringstr3,???),
die drei Zeichenketten alsParameter erhält(beachten Sie,dass der erste Parameter eine Referenz ist)
und void retourniert. Alle Zeichen des ersten Strings,die im zweiten String vorkommen,
sollen durch das Zeichen im dritten String ersetz twerden, das an derselben Position wie 
das zu ersetzende Zeichen im zweiten String steht.Weiters soll die Funktion die Anzahl
der ersetzten Zeichen in einem zusätzlichen Referenzparameter(dessen Definition an Stelle der drei Fragezeichen 
einzufügen ist) zurückliefern. Werfen Sie einen Fehler vom Typ runtime_error, wenn str3kürze rals str2 ist.
Parameter1: halloWelt
Parameter2: lae
Parameter3: ebr
Ergebnis: hbeeoWret5 // hbeeoWret steht in der Referenz zu str1,5 wird im Referenzparameter zurückgeliefert
*/

#include <iostream>
#include <exception>
using namespace std;

int main()
{


}
